# Hash-pinned so CI installs exactly these bytes and nothing else.
#
# Every GitHub Action in this repo is pinned to a commit SHA and every container image to
# a digest; a bare `pip install pyyaml` was the one unreviewed thing that would still run
# in CI, and it is what OpenSSF Scorecard's pinned-dependencies check flags. The hashes
# below cover the manylinux x86_64 wheels for the Python versions a GitHub runner ships,
# plus the sdist as a fallback - pip verifies whichever it selects and refuses anything
# whose bytes do not match.
#
# Refresh with:
#   curl -sS https://pypi.org/pypi/PyYAML/<version>/json | jq -r '.urls[].digests.sha256'
pyyaml==6.0.2 \
    --hash=sha256:3ad2a3decf9aaba3d29c8f537ac4b243e36bef957511b4766cb0057d32b0be85 \
    --hash=sha256:80bab7bfc629882493af4aa31a4cfa43a4c57c83813253626916b8c7ada83476 \
    --hash=sha256:70b189594dbe54f75ab3a1acec5f1e3faa7e8cf2f1e08d9b561cb41b845f69d5 \
    --hash=sha256:d584d9ec91ad65861cc08d42e834324ef890a082e591037abe114850ff7bbc3e
