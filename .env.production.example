# Refuse to start unless both the API and ingest are authenticated.
PG_ENV=production

# PerspectiveGraph - production environment template.
#
# The compose stack reads every knob from `.env`, so hardening it is a matter of
# filling these in. Copy, complete, and lock it down:
#
#   cp .env.production.example .env && chmod 600 .env
#   docker compose --profile app -f docker-compose.yml -f docker-compose.prod.yml up -d
#
# This turns ON every control the demo leaves off. NEVER commit the filled-in .env.
# For Kubernetes, prefer the Helm chart with values-production.yaml.

# ── Authentication (REQUIRED beyond a trusted boundary) ──────────────
# Bearer tokens: "token:role[:tenant]", comma-separated. Generate: openssl rand -hex 24
API_TOKENS=REPLACE_WITH_TOKEN:admin
# ...or OIDC/SSO instead of (or alongside) static tokens; leave the issuer empty to skip.
OIDC_ISSUER=
OIDC_AUDIENCE=
OIDC_JWKS_URL=
OIDC_CLIENT_ID=
OIDC_AUTHORIZE_URL=
# Signing in proves identity; it does not grant a role. Map the groups your directory
# already has, or every SSO user lands with no access. Highest role among the matched
# groups wins; an entry that does not parse is dropped and logged.
OIDC_GROUP_ROLES=pg-viewers=viewer,pg-secops=operator,pg-admins=admin
# Grants a role to EVERY subject the IdP authenticates. Leave empty unless the IdP
# application is itself restricted to the right people.
OIDC_DEFAULT_ROLE=
OIDC_TOKEN_URL=
# Per-client-IP rate limit (requests/sec; 0 disables):
API_RATE_RPS=60

# ── Ingest authenticity (REQUIRED) ───────────────────────────────────
# Scanners must HMAC-sign their webhook bodies. Generate: openssl rand -hex 32
INGEST_HMAC_SECRET=REPLACE_WITH_HMAC_SECRET
INGEST_RATE_RPS=50

# ── Transport security ───────────────────────────────────────────────
# In-app HTTPS: put tls.crt + tls.key in ./tls/ (mounted read-only by
# docker-compose.prod.yml) and point these at them. Or terminate TLS at a reverse
# proxy in front and leave both empty.
TLS_CERT_FILE=/etc/pg/tls/tls.crt
TLS_KEY_FILE=/etc/pg/tls/tls.key
# Restrict browser origins allowed to call the API/dashboard:
CORS_ALLOWED_ORIGINS=https://perspectivegraph.example.com

# ── Database (the connection carries the attack map - encrypt it) ────
# The bundled demo Postgres has no TLS. For sslmode=verify-full use an EXTERNAL
# managed Postgres+AGE and set POSTGRES_HOST to it. Set a strong password either way:
POSTGRES_PASSWORD=REPLACE_WITH_DB_PASSWORD
# POSTGRES_HOST=db.internal
POSTGRES_SSLMODE=require

# ── At-rest encryption + signed exports ──────────────────────────────
# Generate each: openssl rand -hex 32
STORE_ENCRYPTION_KEY=REPLACE_WITH_32_BYTE_HEX
EXPORT_SIGNING_KEY=REPLACE_WITH_32_BYTE_HEX

# ── Tamper-evident audit log (durable) ───────────────────────────────
# Records reads AND writes of the attack map. Verify the chain later with:
#   docker compose exec backend perspectivegraph verify-audit /var/lib/perspectivegraph/audit.log
AUDIT_LOG_PATH=/var/lib/perspectivegraph/audit.log
# How long those records are kept. Empty is not a retention policy, it is the absence
# of one, and it is the first thing a data-protection review asks about. Pruning needs
# GOVERNANCE_BACKEND=postgres, where the chain can be truncated from the front and
# checkpointed so what remains still verifies (verify-audit -postgres).
AUDIT_RETENTION=2160h

# ── Governance state: shared, or on this host's disk ──────────────────
# "postgres" puts the suppressions, tickets, history, validations AND the audit chain
# in the database, so they survive a restart and several replicas share them - and it
# is what makes the brute-force lockout below durable rather than per-process.
# "file" (the default) keeps them next to AUDIT_LOG_PATH, single-writer.
GOVERNANCE_BACKEND=postgres

# ── Brute-force lockout ──────────────────────────────────────────────
# Failed auth attempts from one address within 5 minutes before it is locked out for
# 15. With GOVERNANCE_BACKEND=postgres a lockout survives a deploy; without it, a
# restart clears it - and a release schedule is something anyone can read.
AUTH_LOCKOUT_THRESHOLD=10

# ── Data hygiene (on) ────────────────────────────────────────────────
SCRUB_INGEST=true

# ── Logs (ingestible) ────────────────────────────────────────────────
# json is what a log pipeline wants; text is only for reading in a terminal.
# Every request also carries an id, echoed in X-Request-Id and written into the
# audit record's fields, so one call joins the audit log and the app log.
LOG_FORMAT=json
LOG_LEVEL=info

# ── Ingest coverage ──────────────────────────────────────────────────
# How long a collector may stay silent before the board says so. It is what
# separates "no attack path" from "nothing was ever ingested about that part of
# the estate" - a false negative is otherwise invisible.
COVERAGE_STALE_AFTER=24h

# ── Prefer mounted secrets over these values ─────────────────────────
# Every credential above also accepts <KEY>_FILE holding the PATH to a file:
# API_TOKENS_FILE, INGEST_HMAC_SECRETS_FILE, POSTGRES_PASSWORD_FILE,
# STORE_ENCRYPTION_KEY_FILE, EXPORT_SIGNING_KEY_FILE, GITHUB_TOKEN_FILE, …
#
# A value in this file is a value in the process environment, which
# `docker inspect` prints to anyone in the docker group and which every child
# process inherits. Mounted files avoid that entirely:
#
#   docker compose -f docker-compose.yml -f docker-compose.secrets.yml \
#     --profile app up -d
#
# A <KEY>_FILE that is set but unreadable stops the process - it never falls
# back to a weaker source, because a mistyped path would otherwise start an
# open deployment. See docs/OPERATIONS.md.
